SqlInjectionValidationResult result = guard.ValidateControls(
    txtCustomerName,
    txtAddress,
    txtMobileNo,
    ddlStatus,
    dtFromDate
);

if (!result.IsValid)
{
    throw new ApplicationException(result.Message);
}